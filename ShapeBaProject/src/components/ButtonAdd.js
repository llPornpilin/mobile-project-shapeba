import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View, Button } from 'react-native';

const ButtonAdd = ()=> {
    return (
        <View>
            <Button></Button>
        </View>
    )
}